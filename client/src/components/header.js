import Auth from ',,/utils/auth'

const Header = () => {
    const currentUser = Auth.getLoggedInUser()
    // const toPath = Auth.loggedIn() ?

    return (
        <header className="appHeader">


        </header>
    )
    
}

export default Header